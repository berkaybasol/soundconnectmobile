// organizer_onboarding_page.dart
import 'package:flutter/material.dart';

class OrganizerOnboardingPage extends StatelessWidget {
  const OrganizerOnboardingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Organizatör Onboarding")),
      body: const Center(child: Text("Organizer Profile Onboarding Page")),
    );
  }
}
