// listener_onboarding_page.dart
import 'package:flutter/material.dart';

class ListenerOnboardingPage extends StatelessWidget {
  const ListenerOnboardingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Dinleyici Onboarding")),
      body: const Center(child: Text("Listener Profile Onboarding Page")),
    );
  }
}
