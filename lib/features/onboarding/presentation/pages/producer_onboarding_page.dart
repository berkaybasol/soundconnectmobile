// producer_onboarding_page.dart
import 'package:flutter/material.dart';

class ProducerOnboardingPage extends StatelessWidget {
  const ProducerOnboardingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Prodüktör Onboarding")),
      body: const Center(child: Text("Producer Profile Onboarding Page")),
    );
  }
}
